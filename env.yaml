name: insurance-cloud-env-v4
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - setuptools             # REQUIRED to fix pkg_resources error
      - mlflow==2.11.3
      - mlflow-skinny==2.11.3
      - azureml-mlflow
      - azure-identity         # Required for authentication
      - azure-mgmt-resource
      - numpy==1.26.4
      - pandas==2.2.2
      - scikit-learn
      - xgboost
      - joblib