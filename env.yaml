name: insurance_v5_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - setuptools             # REQUIRED: Fixes 'pkg_resources' crash
      - mlflow==2.11.3
      - azureml-mlflow
      - xgboost
      - scikit-learn
      - pandas
      - numpy
      - joblib